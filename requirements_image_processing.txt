# Requirements for image processing (watermark removal + OCR)

# Image processing
pillow>=9.0.0

# OCR - Option 1: Tesseract (Recommended)
pytesseract>=0.3.10
# Also need Tesseract OCR binary:
# Mac: brew install tesseract
# Linux: sudo apt-get install tesseract-ocr
# Windows: Download from https://github.com/UB-Mannheim/tesseract/wiki

# OCR - Option 2: PaddleOCR (Alternative, more accurate but heavier)
# paddleocr>=2.7.0
# Requires: pip install paddlepaddle paddleocr

# General
numpy>=1.24.0


